# do-rag credentials
# Copy to .env and fill in values, or run ./run.sh to be prompted.
# .env is gitignored and will not be committed.

# Open WebUI admin (required for model import on first run)
OPENWEBUI_ADMIN_EMAIL=admin@local
OPENWEBUI_ADMIN_PASSWORD=your_secure_password
